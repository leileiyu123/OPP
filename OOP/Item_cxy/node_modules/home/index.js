/**
 * Created by lanou on 17/1/10.
 */
// 引入模块只有是当前路径的情况下才需要加"./"
//  否则只需要写模块名称即可，会自动根据module.paths里面
// 的路径数组一次查找，如果到最后还是找不到才会报错，只要在任何一层找到了就会正常引入使用
var home=require("./../node_modules/home/home");
var obj=require("./obj");//接收到的是一个方法模块
console.log(home.first);
console.log(home.second);
home.third(10);


var app=obj();//返回的是对象
app.use();
// console.log(module);
